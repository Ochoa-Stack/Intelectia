<UserControl x:Class="Intelectia.WPF.Views.VendorOnboardingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="480">
        <StackPanel>

            <!-- Ícono y título -->
            <TextBlock Text="🏪" FontSize="56"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,16"/>

            <TextBlock Text="Conviértete en vendedor"
                       Style="{StaticResource TextHeading}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8"/>

            <TextBlock Text="Publica tus libros y llega a miles de estudiantes en Intelectia."
                       Style="{StaticResource TextCaption}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Margin="0,0,0,32"/>

            <!-- Formulario de activación -->
            <Border Background="{StaticResource SurfaceBrush}"
                    CornerRadius="12" Padding="24">
                <StackPanel>

                    <TextBlock Text="Nombre comercial"
                               Style="{StaticResource TextCaption}"
                               Margin="0,0,0,4"/>
                    <TextBox Text="{Binding BusinessName, UpdateSourceTrigger=PropertyChanged}"
                             Background="{StaticResource SurfaceLightBrush}"
                             Foreground="{StaticResource TextPrimaryBrush}"
                             BorderBrush="{StaticResource BorderBrush}"
                             Padding="12,10" BorderThickness="1"
                             Margin="0,0,0,16"/>

                    <TextBlock Text="Descripción (opcional)"
                               Style="{StaticResource TextCaption}"
                               Margin="0,0,0,4"/>
                    <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                             Background="{StaticResource SurfaceLightBrush}"
                             Foreground="{StaticResource TextPrimaryBrush}"
                             BorderBrush="{StaticResource BorderBrush}"
                             Padding="12,10" BorderThickness="1"
                             AcceptsReturn="True" TextWrapping="Wrap"
                             MinHeight="80"
                             Margin="0,0,0,16"/>

                    <!-- Mensaje de error -->
                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="{StaticResource ErrorBrush}"
                               Style="{StaticResource TextCaption}"
                               Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,12"/>

                    <Button Content="Activar mi perfil de vendedor"
                            Command="{Binding ActivateCommand}"
                            Background="{StaticResource PrimaryBrush}"
                            Foreground="{StaticResource TextPrimaryBrush}"
                            BorderThickness="0" Padding="0,12"
                            IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"/>

                </StackPanel>
            </Border>

        </StackPanel>
    </Grid>
</UserControl>
