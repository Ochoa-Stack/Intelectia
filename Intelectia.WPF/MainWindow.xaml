<Window x:Class="Intelectia.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="1280" Height="720"
        MinWidth="1024" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- banner sin conexión -->
            <RowDefinition Height="*"/>     <!-- contenido principal -->
            <RowDefinition Height="20"/>    <!-- footer de créditos -->
        </Grid.RowDefinitions>

        <!-- Banner de sin conexión -->
        <Border Grid.Row="0"
                Background="{StaticResource WarningBrush}"
                Padding="16,8"
                Visibility="{Binding IsOffline, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Path Data="{StaticResource IconWarning}"
                      Fill="#1A1A2E" Width="16" Height="16" Stretch="Uniform"
                      Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock Text="Sin conexión a internet. Algunas funciones pueden no estar disponibles."
                           Foreground="#1A1A2E" FontSize="13" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Contenido principal -->
        <ContentControl Grid.Row="1"
                        x:Name="NavigationFrame"
                        Style="{StaticResource FadeContentControl}"
                        Content="{Binding CurrentViewModel}"/>

        <!-- Sistema de toasts, superpuesto sobre todo el contenido -->
        <ItemsControl Grid.Row="0" Grid.RowSpan="3"
                      ItemsSource="{Binding Toasts}"
                      VerticalAlignment="Bottom"
                      HorizontalAlignment="Right"
                      Margin="0,0,24,24"
                      Panel.ZIndex="999">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="8" Padding="16,12"
                            Margin="0,0,0,8"
                            MinWidth="280" MaxWidth="400">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Type}" Value="Success">
                                        <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Type}" Value="Error">
                                        <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Type}" Value="Warning">
                                        <Setter Property="Background" Value="{StaticResource WarningBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="{Binding Message}"
                                   Foreground="White"
                                   TextWrapping="Wrap"
                                   FontSize="13"/>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Footer de créditos -->
        <Border Grid.Row="2"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0">
            <TextBlock Text="Developed by: Elias Ochoa"
                       FontFamily="{StaticResource FontInter}"
                       FontSize="10"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center"
                       Margin="0,0,16,0"
                       Opacity="0.6"/>
        </Border>

    </Grid>
</Window>
